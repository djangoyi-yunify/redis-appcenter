flush /opt/app/conf/caddy/caddyfile << CADDYFILE_EOF
:80 {
  {{- with getv "/env/web.console.password" "" }}
  basicauth / "{{ getv "/env/web.console.username" "admin" }}" "{{ . }}"
  {{- end }}
  root /data
  gzip
  browse /redis-shake/logs
  tls off
}
CADDYFILE_EOF
